{
  "name": "gigccl-firebase",
  "version": "1.0.0",
  "scripts": {
    "seed": "node seed.js",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:indexes": "firebase deploy --only firestore:indexes"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "firebase-admin": "^12.2.0"
  }
}
